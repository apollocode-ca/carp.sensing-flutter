# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec
name: carp_firebase_backend
description: CARP Mobile Sensing data backend for uploading data to Google Firebase.
version: 0.30.0
homepage: https://github.com/cph-cachet/carp.sensing-flutter/tree/master/backends/carp_firebase_backend
author:  CACHET Team <cph.cachet@gmail.com>

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.8.0
  google_sign_in: ^5.4.4
  firebase_storage: ^11.0.10
  firebase_auth: ^4.2.5
  cloud_firestore: ^4.3.1
  firebase_core: ^2.4.1
  carp_core: ^0.40.6
  carp_mobile_sensing: ^0.40.11


# Overriding carp_mobile_sensing and carp_core to use the local copy
# Remove this when done development
#dependency_overrides:
#  carp_core:
#    path: ../../carp_core/
#  carp_mobile_sensing:
#    path: ../../carp_mobile_sensing/

dev_dependencies:
  build_runner: ^2.0.6       # used to build JSON serialization .g files
  json_serializable: ^4.1.4

# The following section is specific to Flutter.
flutter:

